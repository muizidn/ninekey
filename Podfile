# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'
use_frameworks!

target 'Keyboard' do
#  # Pod from @muizidn
#  pod 'ViewDSL', git: 'https://github.com/muizidn/viewdsl'
#  pod 'FlowConstraint', git: 'https://github.com/muizidn/flowconstraint'
#  pod 'EventClosure', git: 'https://github.com/muizidn/eventclosure'
end

target 'NineKey' do
  target 'NineKeyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'NineKeyUITests' do
    inherit! :search_paths
    # Pods for testing
  end
end
